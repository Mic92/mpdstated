# make it configureable
add optionparser
add Keyfile based config

# documentation
add README
add install instruction

# later features
gui-based configuration
freedesktop compliant autostart
deb and rpm files (using opensuse buildservice?)
synching with other distance (shared json file?)
